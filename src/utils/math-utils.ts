export const integerDivision = (dividend:number, divisor:number) => Math.floor(dividend/divisor);

export const negativeIntegerDivision = (dividend:number, divisor:number) => Math.ceil(dividend/divisor);